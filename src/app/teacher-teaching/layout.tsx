import AppLayout from '@/components/layout/AppLayout';

export default function TeacherTeachingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppLayout>{children}</AppLayout>;
}
